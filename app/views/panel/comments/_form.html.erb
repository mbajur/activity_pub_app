<%= form_with model: ActivityPub::Note.new, url: '' do |f| %>
  <%= f.fields_for :data do |ff| %>
    <%= ff.fields_for :source do |fff| %>
      <div class="border border-gray-300 rounded-sm">
      <%= fff.text_area :content %>
    <% end %>
  <% end %>
<% end %>
