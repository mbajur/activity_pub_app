<turbo-stream action="replace" target="<%= dom_id(@ap_object, :footer) %>">
  <template>
    <%= render partial: 'panel/comments/footer',
               locals: {
                comment: @ap_object,
                liked_comment_ids: [@like.target_ap_object_id]
               },
               formats: [:html] %>
  </template>
</turbo-stream>
